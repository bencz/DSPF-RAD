**FREE

// =====================================================================
// Program CGMULTI
// Generated by DSPF-RAD from CGMULTI
// Source records: FIRSTSFL, FIRSTCTL, SECONDSFL, SECONDCTL
// Edit hint: code inside DSPF-RAD protected-region blocks is
//            preserved across regenerations.  Everything outside the
//            regions is rewritten when you regenerate from the RAD.
// =====================================================================

Ctl-Opt Main(CGMULTI_main) DftActGrp(*No);

Dcl-F CGMULTI Workstn
  SFile(FIRSTSFL:WkSflRrn)
  SFile(SECONDSFL:WkSflRrn02)
  IndDS(WkInd) UsrOpn;
Dcl-S WkSflRrn Packed(5:0) Inz(0);
Dcl-S WkSflRrn02 Packed(5:0) Inz(0);
Dcl-S WkScreen Char(10) Inz('FIRSTCTL');

Dcl-Ds WkInd Qualified Len(99);
  AllIndicators Char(99) Pos(1);
  In03 Ind Pos(3);
  In30 Ind Pos(30);
  In31 Ind Pos(31);
End-Ds;

Dcl-Proc CGMULTI_main;
  Dcl-S done Ind Inz(*Off);

  // [DSPF-RAD-REGION begin=startup]
  // Run-once initialisation (open files, fetch parameters, ...).
  // [DSPF-RAD-REGION end=startup]

  Open CGMULTI;
  Dou done;
    // [DSPF-RAD-REGION begin=before-display]
    // Populate fields for the next display operation.
    // [DSPF-RAD-REGION end=before-display]
    // [DSPF-RAD-REGION begin=select-subfile-screen]
    // Optionally set WkScreen before the generated route selector.
    // [DSPF-RAD-REGION end=select-subfile-screen]
    // WkScreen is managed by the RAD key-action flow.
    Select;
      When WkScreen = 'FIRSTCTL';
        // [DSPF-RAD-REGION begin=load-firstsfl]
        // Fill FIRSTSFL subfile records here.  Suggested skeleton:
        //   WkInd.In30 = *On;  // SFLCLR
        //   Write FIRSTCTL;
        //   WkInd.In30 = *Off;  // reset
        //   Dou %Eof(...);
        //     Read your data source ...
        //     WkSflRrn += 1;
        //     Write FIRSTSFL;
        //   EndDo;
        //   WkInd.In30 = *Off;  // SFLDSP
        //   WkInd.In30 = *Off;  // SFLDSPCTL
        // [DSPF-RAD-REGION end=load-firstsfl]

        Exfmt FIRSTCTL;
      When WkScreen = 'SECONDCTL';
        // [DSPF-RAD-REGION begin=load-secondsfl]
        // Fill SECONDSFL subfile records here.  Suggested skeleton:
        //   WkInd.In31 = *On;  // SFLCLR
        //   Write SECONDCTL;
        //   WkInd.In31 = *Off;  // reset
        //   Dou %Eof(...);
        //     Read your data source ...
        //     WkSflRrn02 += 1;
        //     Write SECONDSFL;
        //   EndDo;
        //   WkInd.In31 = *Off;  // SFLDSP
        //   WkInd.In31 = *Off;  // SFLDSPCTL
        // [DSPF-RAD-REGION end=load-secondsfl]

        Exfmt SECONDCTL;
      Other;
        WkScreen = 'FIRSTCTL';
        Iter;
    EndSl;

    Select;
      When WkInd.In03;
        done = *On;
        // [DSPF-RAD-REGION begin=on-in03]
        // Add custom handling for In03 here.
        // [DSPF-RAD-REGION end=on-in03]
      Other;
        // [DSPF-RAD-REGION begin=on-enter]
        // Handle Enter / data-bearing AID keys with no more specific match.
        // [DSPF-RAD-REGION end=on-enter]
        // Changed rows from FIRSTSFL:
        //   ReadC FIRSTSFL;
        //   Dow Not %Eof(CGMULTI);
        //     // process changed row
        //     ReadC FIRSTSFL;
        //   EndDo;
        // [DSPF-RAD-REGION begin=changed-firstsfl]
        // [DSPF-RAD-REGION end=changed-firstsfl]
        // Changed rows from SECONDSFL:
        //   ReadC SECONDSFL;
        //   Dow Not %Eof(CGMULTI);
        //     // process changed row
        //     ReadC SECONDSFL;
        //   EndDo;
        // [DSPF-RAD-REGION begin=changed-secondsfl]
        // [DSPF-RAD-REGION end=changed-secondsfl]
    EndSl;
  EndDo;

  Close CGMULTI;
  // [DSPF-RAD-REGION begin=shutdown]
  // Cleanup (close files, log, ...).
  // [DSPF-RAD-REGION end=shutdown]
  Return;
End-Proc;
