{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal core capabilities for the main IronTerm Studio window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
