{
  "name": "dspf-rad",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "samples": "node scripts/generate-codegen-samples.js"
  },
  "engines": {
    "node": ">=20"
  }
}
